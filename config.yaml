server:
  enable_rest: true
  rest_port: ":8080"
logger:
  is_dev: true
simulation:
  deterministic: false
  seed: 42
  use_mock_topology: false
  enable_metrics: true
  load_from_save: false
  db_type: "json"